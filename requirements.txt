# Core ML and data processing
torch>=2.0.0
datasets>=2.14.0
transformers>=4.30.0
transformer-lens>=1.0.0
scikit-learn>=1.3.0
pandas>=1.5.0
numpy>=1.24.0

# Utilities and helpers
tqdm>=4.65.0
requests>=2.31.0
textwrap3>=0.9.2
gdown>=4.7.0

# Jupyter/Colab specific (optional for local usage)
# google-colab  # Only needed when running in Google Colab
# huggingface-hub>=0.16.0  # Usually comes with transformers but explicit for token auth

# Additional utilities that might be needed
# einops>=0.6.0  # Commented out in the code but may be needed
# jaxtyping>=0.2.0  # Commented out in the code but may be needed
# colorama>=0.4.0  # Commented out in the code but may be needed